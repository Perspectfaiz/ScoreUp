import testObj from './Testobject.js';

const examObj={
    tag:["All", "Physics", "Chemistry", "Maths"],
    testList:[testObj, testObj, testObj,testObj,testObj,testObj, testObj, testObj,testObj,testObj,testObj, testObj, testObj,testObj,testObj],
}

export default examObj;